name: Deploy to Firebase Hosting on PR

on:
  pull_request_target:
    types:
      - opened
      - synchronize

jobs:
  build_and_preview:
    if: ${{ github.event.pull_request_target.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ASTAR_TOKEN_API }}
          projectId: astar-token-api
          previewChannel: pr-${{ github.event.pull_request.number }}
